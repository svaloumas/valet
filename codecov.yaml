coverage:
  range: 50..90
  round: down
  precision: 2
  status:
    project:
      default:
        # basic
        target: auto
        threshold: 5%
        base: auto
    patch:
      default:
        # basic
        target: auto
        threshold: 5%
        base: auto
